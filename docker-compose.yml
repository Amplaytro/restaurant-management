services:
  mongodb:
    image: mongo:8.0
    container_name: final-evaluation-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: final_evaluation
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
